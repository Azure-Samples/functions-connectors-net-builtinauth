name: azure-functions-m365-email-secured
metadata:
  template: azure-functions-m365-email-secured@0.0.1-alpha
hooks:
  postdeploy:
    windows:
      shell: pwsh
      run: ./infra/scripts/postdeploy.ps1
    posix:
      shell: sh
      run: ./infra/scripts/postdeploy.sh
services:
  function-app:
    project: ./src
    language: dotnet
    host: function
